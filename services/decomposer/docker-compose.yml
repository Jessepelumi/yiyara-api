services:
  engine:
    build:
      context: .
      target: dev # Uses the development stage with Air
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    environment:
      - APP_ENV=development